# Valid Palindrome

## Concept
Two pointers.

## Prompt
Given a string `s`, return `true` if it is a palindrome after converting uppercase letters to lowercase and removing all non-alphanumeric characters.

## Target
Implement `IsPalindrome(s string) bool` in `solution.go`.
