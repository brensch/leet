# Maximum Depth of Binary Tree

## Concept
Tree recursion / DFS.

## Prompt
Given the `root` of a binary tree, return its maximum depth.

## Target
Implement `MaxDepth(root *TreeNode) int` in `solution.go`.
